Voltar ao menu principal: {
  echo -a Menu principal carregado.
  if ($active == Status Window) { .load -ps scripts\popups.ini | halt }
  else if ($active ischan) { .load -pc scripts\popups.ini | halt }
}
-
CORINTHIANS
.Paulista:.
Palmerda
.A:.
-
Quit:.